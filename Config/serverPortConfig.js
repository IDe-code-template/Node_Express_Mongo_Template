/**
 * @fileoverview Module for defining the server configurations.
 * @desc All the major config should be decalred here.
 */

const port = 8000;
const host = "localhost";
const sessionSecret = "winter is coming"
module.exports = {
    port,
    host,
    sessionSecret
}